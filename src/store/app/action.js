import { createAction } from 'redux-actions';

export const SET_USER = createAction('SET_USER');
export const SET_DIRECTIONS_MAP = createAction('SET_DIRECTIONS_MAP');
export const SET_ACTIONS_MAP = createAction('SET_ACTIONS_MAP');
