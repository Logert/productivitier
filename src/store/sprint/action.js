import { createAction } from 'redux-actions';

export const UPDATE_SPRINT_ACTION = createAction('UPDATE_SPRINT_ACTION');
export const ADD_SPRINT = createAction('ADD_SPRINT');
export const UPDATE_DIRECTIONS_SPRINT = createAction('UPDATE_DIRECTIONS_SPRINT');
